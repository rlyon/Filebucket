<% title "Rename Folder" %>
<%= render :partial => "shared/breadcrumbs" %>

<%= render 'form' %>

<p>
	<% if @folder.parent %>
	<%= link_to "Back to '#{@folder.parent.name}' Folder", folders_path(@folder.parent) %>
	<% else %>
	<%= link_to "Back", root_url %>
	<% end %>
</p>
