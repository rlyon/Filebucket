<div id="menu">
<% unless @is_this_folder_being_shared %>
	<ul id="top_menu">
		<% if user_signed_in? %>
		<% if @current_folder %>
		<li>
			<%= link_to "New Folder", new_sub_folder_path(@current_folder) %>
		</li>
		<li>
			<%= link_to "Upload File", new_sub_file_path(@current_folder) %>
		</li>
		<% else %>
		<li>
			<%= link_to "New Folder", new_folder_path %>
		</li>
		<li>
			<%= link_to "Upload File", new_asset_path %>
		</li>
		<% end %>
		<% else %>
		<li>
			<%= link_to "Public Folders", folders_path %>
		</li>
		<% end %>
		<li>
			<%= link_to "Help", "#" %>
		</li>
	</ul>
</div>
<% else %>
	<h3>This folder is being shared to you by <%= @current_folder.user.name %></h3>
<% end %>
